[project]
name = "facebook-ads-mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.6.0",
    "requests>=2.32.3"
]

[project.scripts]
facebook-ads-mcp-server = "server:main"

[tool.uv]
package = true
